// show and hide side menu when menu icon is clicked on mobile devices
const menuIcon = document.getElementById('menuIconMobile');

menuIcon.addEventListener('click', () => {
    
    const sideMenu = document.getElementById('sideMenu');
    if(sideMenu.style.display === 'none'){
        sideMenu.style.display = 'block';
    }else{ 
        sideMenu.style.display = 'none';
    }
}
);

var arr=new Array;
array=document.getElementsByClassName('list-group-item-action');

array.forEach(element => {
    element.addEventListener('click', () => {
        const  gerhard =getElementsByClassName
        
    })
    
});

